import { DirectoryPage } from "../../components/DirectoryPage";

export default function ClientsPage() {
  return <DirectoryPage kind="clients" eyebrow="Client operations" title="Clients" subtitle="Keep commercial context and active delivery in one client-level view." action="Add client" />;
}
